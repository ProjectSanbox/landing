import { Tabs as ReactBootstrapTabs, Tab as ReactBootstrapTab } from 'react-bootstrap'
import styled from 'styled-components/macro'

export const Tabs = styled(ReactBootstrapTabs)``

export const Tab = styled(ReactBootstrapTab)``
